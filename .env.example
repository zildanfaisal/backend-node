# App
NODE_ENV=development
PORT=3000
JWT_SECRET=supersecretkey

# PostgreSQL (local dev). On Railway, prefer POSTGRES_URL or DATABASE_URL
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=postgres
PGPASSWORD=vinsmoke3
PGDATABASE=backend-node
# Or a single URL style (local docker compose, etc)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/backend-node

# Optional
MAX_UPLOAD_MB=5
